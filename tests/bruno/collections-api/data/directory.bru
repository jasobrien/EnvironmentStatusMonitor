meta {
  name: Directory
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/data/directory
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
