meta {
  name: ReadyToDeployEnvTrans
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/readyToDeploy/{{env}}/dashboard
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
