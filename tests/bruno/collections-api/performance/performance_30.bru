meta {
  name: Performance_30
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/dashboard/performance/{{env}}/30
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body: contains Feature Collection Performance
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
